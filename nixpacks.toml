[phases.setup]
nixPkgs = [
  "poppler_utils",
  "python311",
  "python311Packages.pip",
  "gcc",
  "meson",
  "ninja",
  "pkg-config",
  "cairo",

  # Pillow TrueType font support (fixes tiny fallback font on Railway)
  "freetype",
  "fontconfig",
  "dejavu_fonts",
]

[variables]
PYTHON_VERSION = "3.11"
